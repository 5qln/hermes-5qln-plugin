---
name: 5qln-self-evolution
description: Orchestrate bounded self-evolution of the 5QLN Hermes plugin by the plugin — compose skill-v1 formation, bounded parametric memory, and provenance into a self-referential but H-gated evolution loop. Machine drafts and verifies structure; only H originates, recognises, and promotes.
version: 0.1.0
author: 5QLN
license: Proprietary
---

# 5QLN Self-Evolution

> **CANDIDATE {α'} — machine-drafted per G-phase (G = α ≡ {α'}), submitted to the Q-gate.**
> Not validated. Not H-authored. The purpose is H-verbatim (quoted below); the structure is kernel-cited; H confirms or rejects at Q.

## Purpose (H-verbatim, 2026-08-04)

> "Let's iterate toward a Hermes agent-engine skill where 5QLN is not merely a prompt, but an operational self-evolving skill." — H
> "The missing piece between what ships and self-evolution is an orchestrator skill that runs the kernel on the plugin itself, using only shipped machinery. It adds no new authority — it composes existing tools under the nine lines." — H

## Triggers

- H says "evolve the plugin", "form the next skill", "run an evolution cycle"
- H references a return question (∞0') from a prior cycle as the new spark

## Non-Triggers

- Kernel amendment requests — kernel is sealed (kernel.txt, 217 bytes, sha256 feaa46b4…859b); refuse and reflect line 1
- Self-modification without a named H spark (L1)
- Promotion without accepted human review (L3 / V∅)
- Writing review evidence, calibration values, or certification language (L2)

## Authority Boundary

The machine CAN: draft candidate bundles ({α'}), scaffold manifests, run the verifier, compute δE/δV rankings, read calibration via `fiveqln_fractal_memory show`, prepare promotion diffs.
The machine CANNOT: name S, confirm Q, authorise promotion, write review evidence, calibrate the seed, certify anything as living.

## Cycle

1. **S** — Receive H's evolution question verbatim. Reflect only.
2. **G** — Draft candidate bundles as {α'}; hold α = skill-v1 contract + kernel seal. Verify kernel.txt hash == feaa46b4…859b before any draft.
3. **Q** — Present candidates; H review evidence goes to `.verification/evidence/`, scoped to exact `bundle_sha256`. Any bundle change reopens review.
4. **P** — Rank by δE/δV; reveal ∇; H feels the direction.
5. **V** — `verify_skill.py --promotion-mode` after authorisation; promote per formation steps; write ∞0' into CHANGELOG. No V without it.

## Calibration

Read-only: `fiveqln_fractal_memory show`. Calibration is CLI-only with H evidence; this skill never writes calibration values.

## Completion Criteria

- Structural pass + accepted review + authorisation, all scoped to one digest
- ∞0' recorded; corruption log empty (L1–L4, V∅)

## Return Question

What stops the loop from optimising the membrane instead of serving it — evolving K's fluency at the expense of H's origination?
