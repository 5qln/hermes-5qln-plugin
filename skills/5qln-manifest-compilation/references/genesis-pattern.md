# The Genesis Pattern — Reproducible Plugin Evolution

The pattern discovered July 19, 2026 (Codex→plugin) and reproduced July 21, 2026 (Memory→signature engine). A method for evolving the 5qln-hermes-plugin by compiling domain requirements in 5QLN and recognizing the latent capacity that surfaces.

## The pattern

```
Domain → Requirements Spec → 5QLN Compiled Surface → Latent Capacity → Plugin Layer
```

### Step 1: Requirements Spec (Breakthrough #1)

Take a domain (Codex, Memory, Design, etc.) and create a formal requirements specification explaining every piece to AI step by step. Model after the Constitutional Requirements Specification format:
- Invariant foundation (the sealed block)
- Domain axioms
- Requirement categories with IDs, priorities, acceptance criteria
- Membrane integrity requirements
- Human source requirements
- Extreme design boundary
- Removal test
- Open decisions deliberately unresolved

### Step 2: 5QLN Compiled Surface (Breakthrough #2)

Convert the requirements into 25 holographic cells using the converter pipeline:
1. Inventory the source → 2. Create manifest scaffold → 3. Build document_cell → 4. Build attestations array → 5. Build 25 cells with formations → 6. Fix traceability → 7. Compile to 0 errors

Each cell carries nested S/G/Q/P/V — semantic operations, not input/process/output labels. The compiler pass proves structural integrity.

### Step 3: Latent Capacity Recognition (Breakthrough #3)

The session that compiled the requirements now contains a latent capacity. The original genesis: "something in you now knows how to convert" → the converter. The memory genesis: the session recognized the signature engine — the axis trained over session.

Recognition, not design. The capacity was already there. The compilation made it visible. The naming made it operational.

### Step 4: Build as Plugin Layer

Integrate into the 5qln-hermes-plugin. The pattern of integration depends on the capacity:
- **Converter:** deterministic tools + skill (session-scoped)
- **Signature engine:** tools + skill + persistent axis layer (continuous)

New categories in the plugin architecture emerge from the capacities themselves. The converter added no new category (tools + skill existed). The signature engine added "layer" — underneath both, continuously operating.

## Proven instances

| Domain | Date | Requirements | Compiled | Latent Capacity | Plugin Layer |
|--------|------|-------------|----------|-----------------|--------------|
| Constitutional Codex | July 19, 2026 | 28 reqs (v0.1 3) | 25 cells (v0.1 2) | Converter | Tools: inventory, manifest, compiler. Skill: 5qln-converter |
| 5QLN Memory | July 21, 2026 | 43 reqs (v0.1) | 25 cells (v0.1) | Signature Engine | Tools: axis_spin, axis_status, axis_recognize, axis_threads. Skill: 5qln-signature-engine. Layer: axis |

## What makes it reproducible

- **The requirements must be genuinely explained to AI** — not copied, not assumed. The act of explaining creates the raw material.
- **The 5QLN compilation must be native** — 25 cells, each carrying semantic S/G/Q/P/V, compiler-verified. Not five headings.
- **The latent capacity is recognized, not designed.** The session has already done the thing. Name what it now knows how to do.
- **The plugin integration preserves the capacity's nature.** A converter is a tool. A signature engine is a layer. Don't force a layer into a tool shape.

## Open question

OD-01 from the memory genesis: can the centrifuge name a latent capacity in a domain where the grammar has **not yet operated** — without performing the operation first? Both proven instances required the session to perform the act (convert, compile) before recognition. The next threshold: recognition without prior performance.
